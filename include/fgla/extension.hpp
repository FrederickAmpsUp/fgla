#pragma once

#include <fgla/util.hpp>
#include <optional>

namespace fgla::extension {

/// A Universally Unique Identifier identifying an extension
using ExtensionUUID = util::UUID<16>;

} // namespace fgla::extension
